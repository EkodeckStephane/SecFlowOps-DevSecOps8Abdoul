# Controlled Repositories

`sample_api` is a local defensive test application. It contains intentionally
injected findings so scanner behavior can be measured against a traceable ground
truth. Do not deploy it publicly.
